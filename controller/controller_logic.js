// Define the displayCartItems function to handle requests to /cart_items
const displayCartItems = (req, res) => {
    res.send('Cart Items Displayed!'); // Send a response with the message "Cart Items Displayed!"
};

// Export the displayCartItems function as the default export
export default displayCartItems;